apiVersion: rules.spotter.dev/v1alpha1
kind: SpotterRule
metadata:
  name: spotter-workload-003
  labels:
    severity: "critical"
    category: "workload"
  annotations:
    rules.spotter.dev/title: "Shared Host IPC Namespace"
    rules.spotter.dev/version: "1.0.0"
    rules.spotter.dev/cwe: "CWE-265"
    rules.spotter.dev/description: "Pods that use the host IPC namespace can access the node's IPC, which can be a security risk."
spec:
  match:
    resources:
      kubernetes:
        apiGroups:
          - ""
          - "apps"
          - "serving.knative.dev"
        versions:
          - "v1"
        kinds:
          - Pod
          - Deployment
          - StatefulSet
          - DaemonSet
          - Configuration
        namespaces:
          include: ["*"]
          exclude: ["kube-system", "kube-public"]
        labels:
          exclude: 
            rules.spotter.dev/ignore: ["true"]
  cel: |
    (object.kind == 'Pod' && has(object.spec.hostIPC) && object.spec.hostIPC == true) ||
    (object.kind == 'Configuration' && has(object.spec.template.spec.hostIPC) && object.spec.template.spec.hostIPC == true) ||
    (object.kind in ['Deployment', 'StatefulSet', 'DaemonSet'] && has(object.spec.template.spec.hostIPC) && object.spec.template.spec.hostIPC == true)
  remediation:
    manual: "Do not use the host IPC namespace for pods unless it is absolutely necessary."
  references:
    - title: "Pod Security Standards"
      url: "https://kubernetes.io/docs/concepts/security/pod-security-standards/#host-namespaces"
