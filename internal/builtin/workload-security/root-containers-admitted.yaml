apiVersion: rules.spotter.run/v1
kind: SecurityRule
metadata:
  name: root-containers-admitted
  labels:
    category: "Workload Security"
    severity: critical
spec:
  id: SPOTTER-WORKLOAD-SECURITY-585
  name: "Root Containers Admitted"
  version: "1.0.0"
  description: "Containers must not run as UID 0"
  severity:
    level: CRITICAL
    score: 9.0
  category: "Workload Security"
  subcategory: "Pod Security Context"
  cwe: "CWE-585"
  regulatoryStandards:
    - name: "CIS Kubernetes Benchmark v1.8.0"
      reference: "https://www.cisecurity.org/benchmark/kubernetes"
      section: "5.2.6"
  match:
    resources:
      kubernetes:
        apiGroups:
          - "policy"
        versions:
          - "v1beta1"
        kinds:
          - PodSecurityPolicy
        namespaces:
          include: ["*"]
          exclude: []
  cel: |
    object.kind == "PodSecurityPolicy" && has(object.spec.runAsUser) && object.spec.runAsUser.rule == "RunAsAny"
  remediation:
    manual: "Set runAsNonRoot=true and runAsUser>=1000"
  metadata:
    author: "Spotter AutoGen"
    created: "2025-07-29"
