apiVersion: rules.spotter.run/v1
kind: SecurityRule
metadata:
  name: net-raw-capabilities-not-being-dropped
  labels:
    category: "Workload Security"
    severity: high
spec:
  id: SPOTTER-WORKLOAD-SECURITY-306
  name: "NET_RAW Capabilities Not Being Dropped"
  version: "1.0.0"
  description: "NET_RAW capability must be dropped"
  severity:
    level: HIGH
    score: 8.0
  category: "Workload Security"
  subcategory: "Container Capabilities"
  cwe: "CWE-306"
  regulatoryStandards:
    - name: "CIS Kubernetes Benchmark v1.8.0"
      reference: "https://www.cisecurity.org/benchmark/kubernetes"
      section: "5.2.11"
  match:
    resources:
      kubernetes:
        apiGroups:
        - ''
        - apps
        - policy
        versions:
        - v1
        - v1beta1
        kinds:
        - Pod
        - Deployment
        - StatefulSet
        - PodSecurityPolicy
        namespaces:
          include:
          - '*'
          exclude:
          - kube-system
          - kube-public
  cel: |
    (object.kind == 'PodSecurityPolicy' && has(object.spec.requiredDropCapabilities) && !object.spec.requiredDropCapabilities.exists(cap, cap == "NET_RAW")) ||
    (object.kind == 'Pod' && (
      (has(object.spec.containers) && object.spec.containers.exists(c, 
        !has(c.securityContext) || !has(c.securityContext.capabilities) || !has(c.securityContext.capabilities.drop) || !c.securityContext.capabilities.drop.exists(cap, cap == "NET_RAW")
      )) ||
      (has(object.spec.initContainers) && object.spec.initContainers.exists(c, 
        !has(c.securityContext) || !has(c.securityContext.capabilities) || !has(c.securityContext.capabilities.drop) || !c.securityContext.capabilities.drop.exists(cap, cap == "NET_RAW")
      ))
    )) ||
    (object.kind != 'Pod' && object.kind != 'PodSecurityPolicy' && (
      (has(object.spec.template.spec.containers) && object.spec.template.spec.containers.exists(c, 
        !has(c.securityContext) || !has(c.securityContext.capabilities) || !has(c.securityContext.capabilities.drop) || !c.securityContext.capabilities.drop.exists(cap, cap == "NET_RAW")
      )) ||
      (has(object.spec.template.spec.initContainers) && object.spec.template.spec.initContainers.exists(c, 
        !has(c.securityContext) || !has(c.securityContext.capabilities) || !has(c.securityContext.capabilities.drop) || !c.securityContext.capabilities.drop.exists(cap, cap == "NET_RAW")
      ))
    ))
  remediation:
    manual: "Drop NET_RAW capability in securityContext"
  metadata:
    author: "Spotter AutoGen"
    created: "2025-07-29"
