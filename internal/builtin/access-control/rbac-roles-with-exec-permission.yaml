apiVersion: rules.spotter.run/v1
kind: SecurityRule
metadata:
  name: rbac-roles-with-exec-permission
  labels:
    category: "Access Control"
    severity: high
spec:
  id: SPOTTER-ACCESS-CONTROL-923
  name: "RBAC Role Allows Exec"
  version: "1.0.0"
  description: "RBAC roles must not allow exec"
  severity:
    level: HIGH
    score: 8.0
  category: "Access Control"
  subcategory: "RBAC Permissions"
  cwe: "CWE-266"
  regulatoryStandards:
    - name: "NIST SP 800-53"
      reference: "https://csrc.nist.gov/publications/detail/sp/800-53/rev-5/final"
      section: "AC-6"
  match:
    resources:
      kubernetes:
        apiGroups:
        - rbac.authorization.k8s.io
        versions:
        - v1
        kinds:
        - Role
        - ClusterRole
        namespaces:
          include:
          - '*'
          exclude:
          - kube-system
          - kube-public
  cel: |
    object.kind in ["Role", "ClusterRole"] && object.rules.exists(r, 
      "exec" in r.verbs || 
      (has(r.resources) && r.resources.exists(res, res.endsWith("/exec")))
    )
  remediation:
    manual: "Remove exec verbs from RBAC"
  metadata:
    author: "Spotter AutoGen"
    created: "2025-07-29"
