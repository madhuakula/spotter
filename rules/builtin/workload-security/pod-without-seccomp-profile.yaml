apiVersion: rules.spotter.run/v1
kind: SecurityRule
metadata:
  name: pod-without-seccomp-profile
  labels:
    category: "Workload Security"
    severity: high
spec:
  id: SPOTTER-WORKLOAD-SECURITY-025
  name: "Pod Without Seccomp Profile"
  version: "1.0.0"
  description: "All pods must define seccompProfile"
  severity:
    level: HIGH
    score: 8.0
  category: "Workload Security"
  subcategory: "Pod Security Context"
  cwe: "CWE-024"
  regulatoryStandards:
    - name: "CIS Kubernetes Benchmark v1.8.0"
      reference: "https://www.cisecurity.org/benchmark/kubernetes"
      section: "5.2.7"
  match:
    resources:
      kubernetes:
        apiGroups:
        - ''
        - apps
        versions:
        - v1
        kinds:
        - Pod
        - Deployment
        - StatefulSet
        namespaces:
          include:
          - '*'
          exclude:
          - kube-system
          - kube-public
  cel: |
    (object.kind == 'Pod' && has(object.spec.containers) && object.spec.containers.exists(c, !has(c.securityContext) || !has(c.securityContext.seccompProfile) || c.securityContext.seccompProfile.type != "RuntimeDefault")) ||
    (object.kind != 'Pod' && has(object.spec.template.spec.containers) && object.spec.template.spec.containers.exists(c, !has(c.securityContext) || !has(c.securityContext.seccompProfile) || c.securityContext.seccompProfile.type != "RuntimeDefault"))
  remediation:
    manual: "Set seccompProfile: RuntimeDefault"
  metadata:
    author: "Spotter AutoGen"
    created: "2025-07-29"